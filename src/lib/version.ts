/**
 * Single source of truth for the app version.
 *
 * Update ONLY this file when bumping the version — every other module
 * should import { APP_VERSION } from "@/lib/version".
 */
export const APP_VERSION = "2.6.0";
export const APP_NAME = "ClinicNest";
